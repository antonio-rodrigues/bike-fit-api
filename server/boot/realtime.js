let es = require('event-stream')

module.exports = function (app) {
  // const Notification = app.models.Notification
  // Notification.createChangeStream((err, changes) => {
  //   if (err) { console.error('> Notification.createChangeStream', err) }
  //   changes.pipe(es.stringify()).pipe(process.stdout)
  // })

  // const Operation = app.models.Operation
  // Operation.createChangeStream((err, changes) => {
  //   if (err) { console.error('> Operation.createChangeStream', err) }
  //   changes.pipe(es.stringify()).pipe(process.stdout)
  // })
}
